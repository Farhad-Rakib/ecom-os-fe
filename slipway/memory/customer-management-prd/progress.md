# Progress
- 2026-09-08 — plan-product — prd: approved (3 features). New variant `customer-management-prd` mirrored from `ecom-os-be`'s. This repo's scope: a customer list + detail page (Feature 1), deactivate/reactivate on that detail page (Feature 2), and the recipient view + rating display on `order-management-prd`'s existing Orders pages (Feature 3, same additive pattern as `store-sync-prd`'s `SyncedBadge`). Split by dependency and must not be grouped as one block: Features 1-2 need only the customer admin endpoints; Feature 3 needs `courier-prd` shipped end to end. The rating's presentation is an acceptance criterion, not styling — never a bare percentage, always the delivered/returned/decided counts beside it, and "insufficient history" visibly distinct from a poor ratio, because a blank reads as "fine" to someone moving fast. See `slipway/memory/customer-management-prd/decisions.md`.
- 2026-09-08 — shape-spec — task-groups: approved (2 groups: 01-customer-directory-ui, 02-recipient-and-rating-ui). Group 02 depends on `courier-prd`'s FE group 02 since both edit the same two Orders files. Key decision in `decisions.md`: the PRD's rating presentation rules (never a bare percentage, counts always beside the ratio, insufficient-history visibly distinct from a poor score) are enforced in a single `RatingBadge` component rather than at each of the three call sites — three implementations is three chances to get one wrong, and the failure mode being guarded against is an admin reading a blank as "fine".
- 2026-09-08 — implement — task-group-01: Customer Directory & Status UI shipped. `CustomersPage.tsx` (paged, one search box across email/name/phone matching the backend's single-term search, include-inactive toggle) and `CustomerDetailPage.tsx` (profile, addresses, order history linking to `/orders/{id}`, status panel with its audit trail, deactivate/reactivate behind a `ConfirmDialog` that states the session consequence plainly — the backend signs the customer out everywhere immediately). The status audit row is absent rather than blank for an untouched account. Routes under `customers.manage`. `tsc --noEmit`/`vite build` clean.
- 2026-09-08 — implement — task-group-02: Recipient & Rating Surfaces shipped. New `RatingBadge.tsx` — the only component in this repo that renders a rating — enforcing the PRD's three presentation rules in one place: never a bare percentage, counts always beside the ratio, and three genuinely distinct states (a real score / "Not enough history (n of m)" / "No delivery history"). New `RecipientPage.tsx` (phone lookup in any format, recipient view with names ordered under, linked account, rating, and the orders behind it with per-order outcome). `OrdersPage.tsx` gained a Delivery rating column fed by ONE `POST /admin/ratings/batch` per page render, never per row; `OrderDetailPage.tsx` shows the rating beside the shipping address with a link into the recipient history. A 400 renders as a validation message distinct from a valid number with no history. Backend `CustomerManagementModule` gained a second `MenuDefinition` for Recipient Lookup so the page has a sidebar entry. Nothing blocks or warns on dispatch — informational in v1, per the PRD. `tsc --noEmit`/`vite build` clean.
