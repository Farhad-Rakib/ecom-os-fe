# Progress

- 2026-08-19 — plan-product — prd: approved (2 features). New variant `store-sync-prd` ("External Store Sync") created in this repo — admin control surface (connect/disconnect/status/manual-sync) for the already-implemented `ecom-os-be` `store-sync-prd` backend (all 3 task groups). Feature 1: `StoreConnectionPage.tsx`. Feature 2: a small "Synced from Shopify" indicator amendment to `order-management-prd`'s `OrdersPage.tsx`/`OrderDetailPage.tsx` (that variant's own PRD left this out deliberately so this variant could own it — same amendment pattern the backend PRDs used on each other). Flagged, not solved: proactive read-only marking of synced products/categories in the existing Catalog admin pages needs a backend DTO change (`ProductDto`/`CategoryDto` have no sync-origin field today) — left out of scope, existing 409-on-edit + generic error toast already covers correctness, just not proactively.
- 2026-08-19 — shape-spec — task-groups: approved (2 groups: 01-store-connection, 02-synced-order-indicator). `StoreConnectionPage.tsx` in `src/features/store-sync/`, settings-page shape (closer to `SiteSettingsPage.tsx` than a list page) with a `SUPPORTED_PLATFORMS` constant (one entry, Shopify) so the selector doesn't assume Shopify is permanent. Group 02 is a small additive edit to `order-management-prd`'s already-built `OrdersPage.tsx`/`OrderDetailPage.tsx`, split into its own group specifically because it has a hard build-order dependency those files exist first. Full reasoning in `decisions.md`.
- 2026-08-19 — implement — task-group-01: `StoreConnectionPage.tsx` built (form/status/sync-now/disconnect, all inline per the task group's own spec) plus an additive `store-sync/connection` route in `router/index.tsx`. `tsc --noEmit`/`vite build` both clean (one pre-existing, unrelated `tsc` error in `BrandingPage.tsx`). Discrepancy flagged, not worked around: the task group assumes a static FE nav-config file for the sidebar entry, but this repo's sidebar is entirely backend-driven (`menuApi`/`MenuPage.tsx`) — no such file exists, so no sidebar link was added in code; see `developer-report.md` for detail.
- 2026-08-19 — implement — task-group-02: done, uncommitted. Added an exported `SyncedBadge` component to `order-management-prd`'s `OrdersPage.tsx` ("Synced from Shopify" pill), rendered next to `buyerLabel(order)` in both `OrdersPage.tsx`'s buyer column and `OrderDetailPage.tsx`'s header, gated on `order.customerId === null`. No new files, no API changes, exactly as scoped. `tsc --noEmit`/`vite build` both clean (same pre-existing `BrandingPage.tsx` error, unrelated). `store-sync-prd` is now fully implemented (both task groups) on this repo's admin side, same as its backend counterpart.
