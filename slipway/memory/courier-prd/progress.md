# Progress
- 2026-09-08 — plan-product — prd: approved (3 features). New variant `courier-prd` mirrored from `ecom-os-be`'s — this repo's scope is the admin control surface: a courier directory page, a settings-shaped connection/credentials page (masked inputs, test-connection), and dispatch + delivery-status as additive edits to `order-management-prd`'s already-built `OrdersPage.tsx`/`OrderDetailPage.tsx` rather than a separate dispatch screen. Unlike every prior variant in this repo, the backend is **not yet implemented** — `/shape-spec` here should wait until the backend's contracts are settled rather than hand-mirroring DTO shapes that will change. v1 connector is Steadfast, but the courier selector must stay data-driven (same call `store-sync-prd` made with `SUPPORTED_PLATFORMS`). Third occurrence of the backend-driven-sidebar gap: no static nav config file exists, so new pages need a `MenuItem` row via Menu Management or a seed. See `slipway/memory/courier-prd/decisions.md`, and `ecom-os-be`'s for the full product reasoning.
- 2026-09-08 — shape-spec — task-groups: approved (2 groups: 01-courier-directory-and-connection, 02-dispatch-on-orders). Group 02 has a hard build-order dependency on 01 (its courier picker imports from `CouriersPage.tsx`). Two things recorded in `decisions.md`: the "no static nav config" gap does NOT apply to these pages — `CourierModule` declares its own `MenuDefinition` and modules are discovered by assembly scan and seeded, so the sidebar entry arrives from the backend automatically; and group 02 carries a **required** maintenance update to `OrdersPage.tsx`'s hand-mirrored `OrderStatus`/`ALLOWED_TRANSITIONS`, since the backend's courier work added `Paid -> Shipped` and the new terminal `Returned` state.
- 2026-09-08 — implement — task-group-01: Courier Directory & Connection UI shipped. `CouriersPage.tsx` (list, create/edit modal, activate/deactivate, `SUPPORTED_INTEGRATIONS` driving the integration selector so Pathao/RedX later is a data addition) and `CourierConnectionPage.tsx` (settings-shaped, masked credential inputs cleared from component state on success, test-connection surfacing the backend's own message, disconnect). A `Manual` courier shows no credential fields, no warning, and no Connection row action — the backend's `NotApplicable` status is honoured as genuinely distinct from `NotConnected`. Changing a courier's integration warns that saved credentials will be discarded, mirroring the backend. Routes under `courier.manage`. `tsc --noEmit`/`vite build` clean.
- 2026-09-08 — implement — task-group-02: Dispatch & Delivery Status shipped. New `DispatchPanel.tsx` (consignment DTOs, `consignmentApi`, dispatch/manual-dispatch forms, consignment detail, delivery-event timeline, and Record Outcome for a manual courier's in-flight consignment), added to `OrderDetailPage.tsx`; recipient phone surfaced in the address block. Applied the **required** update to `OrdersPage.tsx`'s hand-mirrored backend constants — `OrderStatus` gains `Returned`, `Paid → Shipped` and `Shipped → Returned` added to `ALLOWED_TRANSITIONS`, `Returned` terminal, badge class in orange rather than Cancelled's neutral grey. The client-side precondition mirror explains why an order cannot be dispatched rather than authorizing it; a 404 on `GET /orders/{id}/consignment` is the ordinary not-dispatched case and never surfaces as an error. Courier-reported vs staff-recorded outcomes read differently everywhere, per PRD Feature 5 AC6. `tsc --noEmit`/`vite build` clean.
