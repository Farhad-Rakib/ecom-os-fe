# Progress

- 2026-08-05 — plan-product — prd: approved (9 features)
- 2026-08-05 — shape-spec — task-groups: approved (10 groups: 01-taxonomy-brands, 02-core-product-publishing, 03-attribute-engine, 04-variants-merchandising, 05-multi-location-inventory, 06-pricing-tax-sales, 07-digital-products, 08-localization, 09-reviews-ratings, 10-storefront-catalog-api)
- 2026-08-05 — implement — task-group-01: Taxonomy/Category/Brand entities, admin API, and admin UI shipped on branch catalog/task-group-01-taxonomy-brands (both repos); 81/81 backend tests passing, FE typecheck/build clean.
- 2026-08-06 — implement — task-group-02: Product/ProductVariant/ProductMedia/ProductCategory entities, publish/archive workflow, media upload (local disk storage), and admin UI (ProductsPage + bespoke ProductEditorPage) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 121/121 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-02/developer-report.md (BE repo) for deviations (variant price/stock folded into product DTOs, added GET /products/types, first uploaded image auto-primary).
- 2026-08-06 — implement — task-group-03: Attribute engine (AttributeGroup/Definition/Option/Set/SetAttribute/ProductAttributeValue), category/product attribute-set binding, publish now blocks on missing required attributes, and admin UI (AttributesPage, AttributeSetsPage + builder, AttributeValueForm wired into ProductEditorPage's new Specifications section) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 150/150 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-03/developer-report.md (BE repo) for deviations.
- 2026-08-06 — implement — task-group-04: Variant-matrix generation (Cartesian over variant-defining attributes, idempotent), ProductOption/OptionValue, ProductBundleItem, ProductRelation, SizeChart/SizeChartRow, and admin UI (VariantMatrixGrid, options/bundle/relations sections in ProductEditorPage, SizeChartsPage) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 169/169 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-04/developer-report.md (BE repo) for deviations.
- 2026-08-06 — implement — task-group-05: Multi-location inventory (Warehouse, InventoryItem), retired ProductVariant.QuantityOnHand (data-preserving migration), and admin UI (WarehousesPage, InventoryPage with low-stock indicator; VariantMatrixGrid/ProductEditorPage now link out to Inventory instead of inline stock editing) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 185/185 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-05/developer-report.md (BE repo) for deviations.
- 2026-08-06 — plan-product — PRD narrowed to pure product management/configuration scope at user request: Features 5 (Multi-Location Inventory), 6 (Pricing/Tax/Sales), 8 (Localization & Reviews), 9 (Storefront Catalog API) descoped from prd.md's forward-looking scope (both repos). Feature 5 was already implemented as Task Group 05 -- that code is untouched, only the PRD's active feature list changed. Feature 4 (Variants) trimmed in the PRD text to the SKU-matrix-generation acceptance criteria only; its already-shipped bundles/relations/size-charts remain in the codebase as delivered history. Removed now-orphaned task-group spec files 06/08/09/10 (never implemented) from slipway/product/task-groups/ in both repos.
- 2026-08-06 — security-review — Backend-only fix (no FE changes): a tenant-isolation audit found and fixed 9 cross-tenant IDOR gaps in catalog services (options, bundle items, relations, media, attribute options, size-chart rows) that validated a child row's FK against a URL parameter without ever confirming that URL parameter belonged to the current tenant. See slipway/memory/decisions.md (BE repo) for full details; BE suite 197/197 passing after the fix.
- 2026-08-07 — implement — task-group-07: Digital Products. ProductVariant.IsDigital, tenant-scoped DigitalAsset (unique per variant), GET/PUT /products/{id}/variants/{variantId}/digital-asset, PublishAsync now blocks a digital variant with no digital asset. Admin UI: Digital checkbox + Digital Delivery modal (file URL/name, max downloads, expiry days, license key pool id) added to VariantMatrixGrid. Shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 203/203 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-07/developer-report.md (BE repo) for deviations.
- 2026-08-07 — qa — Full first QA pass across all 6 catalog task groups (01-05, 07), run in the BE repo -- reports at slipway/reports/task-group-<NN>/qa-report.md (BE repo). Real defects found: TG01 delete-with-products-attached returns 500 not 409; TG02 never implemented the PRD's Draft→Pending Review→Published lifecycle; TG03 has no FE UI at all to bind an attribute set to a category/product (API-only, its core deliverable is unreachable through the app); TG07 blank digital-asset fields return 500 not 400 (same missing-validation pattern as TG01). TG04/TG05 clean, no defects. See slipway/memory/progress.md (BE repo) for the full per-group breakdown.
- 2026-08-07 — implement — defect-fix pass: fixed every defect the QA pass above found. FE changes: CategoriesPage.tsx and ProductEditorPage.tsx now expose an attribute-set picker (category default, product override), closing TG03's "API-only, no way to actually use it" finding; PendingReview status + a "Submit for Review" action added to ProductsPage.tsx and ProductEditorPage.tsx (TG02's missing lifecycle step); explicit client-side file-type check on image upload (TG02 minor); attribute number inputs now show/enforce their min/max bounds (TG03 minor); a delete action was added to TaxonomiesPage.tsx now that the backend endpoint exists (TG01). Backend-side fixes (exception mapping, delete guards, PendingReview status/endpoint) are in the BE repo -- see its progress.md/decisions.md. Typecheck and build both clean. Uncommitted.
- 2026-08-07 — plan-product — prd: approved (5 features). slipway/product/prd.md (both repos) replaced entirely -- now the Storefront Configuration Module PRD (Branding & Site Identity, Content Pages, Product Showcases/Collections, Homepage Hero Banner, Navigation Menus), not Catalog. Catalog's own PRD content is superseded going forward but all its shipped code, task-group specs, and developer/QA reports remain untouched as historical record (same pattern as the 2026-08-06 Catalog rescoping). Deliberately excludes a public read API and the storefront frontend itself, both deferred to when that project starts, mirroring Catalog's own Feature 9 deferral. tech-stack.md updated to match (both repos).
- 2026-08-07 — shape-spec — task-groups: approved (5 groups: 11-storefront-branding, 12-content-pages, 13-product-showcases, 14-homepage-hero-banner, 15-navigation-menus). Numbered from 11 to avoid colliding with Catalog's historically-referenced-but-deleted task-group numbers. New StorefrontConfigModule.cs introduced rather than reusing the existing Menu/SiteSetting entities (those configure the admin app itself, not the tenant's storefront). See slipway/memory/decisions.md (BE repo) for the full design reasoning, including the shared LinkTargetType approach Task Groups 14/15 use.
- 2026-08-09 — implement — task-group-11: Storefront Branding & Site Identity FE. New `BrandingPage.tsx` (single settings form: site title, tagline, 6 social links, independent favicon/logo `ImageUpload` zones backed by dedicated multipart upload endpoints). Router route + Sidebar `iconMap` extended (store/palette/file-text/layout-grid/image) so the backend-driven "Storefront" menu section renders icons at all. Backend was already implemented (`ecom-os-be`). `tsc`/`vite build` clean; no live-backend click-through this pass (see developer-report.md). See slipway/reports/default/task-group-11/developer-report.md.
- 2026-08-09 — implement — task-group-12: Content Pages FE. New `ContentPagesPage.tsx` (list, publish/unpublish/delete) + `ContentPageEditorPage.tsx` (full-page editor, slug auto-derives from title until manually touched, plain-textarea body per the task group's own endorsed fallback). Router routes added. `tsc`/`vite build` clean. See slipway/reports/default/task-group-12/developer-report.md.
- 2026-08-09 — implement — task-group-13: Product Showcases (Collections) FE. New `CollectionsPage.tsx` (Create/Rename/Delete) + `CollectionEditorPage.tsx` (product search-and-add via Autocomplete, up/down reorder). Router routes added. `tsc`/`vite build` clean. See slipway/reports/default/task-group-13/developer-report.md.
- 2026-08-09 — implement — task-group-14: Homepage Hero Banner FE. New shared `LinkTargetPicker` component (kind selector + dependent picker per target type, `allowedKinds`-parameterized for reuse) and `HeroBannerPage.tsx` (image upload, headline/subtext, CTA restricted to Category/Product/Collection/CustomUrl + allowNone, matching the backend validator exactly; shows a `targetMissing` warning banner). `LinkTargetType`'s int values were hardcoded against a direct read of the backend enum/JSON config (confirmed no `JsonStringEnumConverter` is registered — it serializes as a raw int). Router route added. `tsc`/`vite build` clean. See slipway/reports/default/task-group-14/developer-report.md.
- 2026-08-09 — implement — task-group-15: Navigation Menus FE. New `StorefrontMenusPage.tsx` — Header/Footer tabs, client-assembled one-level tree from the location's flat item list, up/down reorder + indent/outdent (explicit buttons, matching the task group's own recommendation over drag-and-drop), whole-location-replace reorder submission per the API's documented contract, delete confirmation names child count before a cascade. Reuses Task Group 14's `LinkTargetPicker` with all 7 kinds enabled, no `allowNone` (backend's `LinkTargetType` is non-nullable there). Router route added. `tsc`/`vite build` clean. See slipway/reports/default/task-group-15/developer-report.md.
- 2026-08-09 — implement — Storefront Configuration FE (task groups 11-15) complete: all 5 admin feature areas (Branding, Content Pages, Collections, Hero Banner, Navigation Menus) now have working admin UI wired to the already-shipped backend. None of this pass was exercised against a live backend (no local Postgres available in-session) -- verification was `tsc --noEmit`/`vite build` only, consistent with this project's established FE-only verification bar. A `/qa` or manual click-through pass against a running `ecom-os-be` is the natural next step before considering the module done end-to-end.
- 2026-08-09 — scope-feature — feature 6: Public Storefront Read API (Menus & Product Listing) approved (mirrored from ecom-os-be) — anonymous (Host-header-resolved tenant, no auth) read access to header/footer menus (link targets resolved inline to name+URL) and a paginated/filterable product listing (category, brand, search) plus single-product detail by slug, Published-only. Explicitly excludes Branding/Content Pages/Hero Banner/Collections public endpoints (follow-up candidate), any writes, and the storefront frontend itself. Narrows the PRD's product-wide "no public read API" out-of-scope bullet accordingly. Ready for `/shape-spec` to design the actual implementation (this command made no technical choices) -- backend-only work, no FE task group implied yet.
- 2026-08-10 — scope-feature — feature 7: Multi-Banner Hero Carousel & Public Storefront Read API (Branding, Hero Banners & Brand Directory) approved (mirrored from ecom-os-be) — this repo's share is converting `HeroBannerPage.tsx` (Task Group 14) from a single-resource upsert form to a list view (create/edit/delete/reorder), once the backend's Hero Banner API becomes list-shaped; the public read endpoints themselves and the domain/migration work are entirely `ecom-os-be`'s scope. Reuses `LinkTargetPicker` unchanged. Ready for `/shape-spec`.
- 2026-08-10 — shape-spec — task-groups: approved (1 group: 16-hero-banner-list-ui). `HeroBannerPage.tsx` rewritten to mirror `StorefrontMenusPage.tsx`'s list+reorder+modal pattern (up/down buttons, whole-list reorder submission, `ConfirmDialog` delete, shared `HeroBannerForm` in a `Modal`) — the closest existing precedent for an ordered per-tenant list. Image upload becomes edit-mode-only (needs a saved row's `id`). No routing change. See slipway/memory/decisions.md for full reasoning, including why the route path stays singular (`storefront/hero-banner`) despite the API base path becoming plural.
- 2026-08-10 — implement — task-group-16: Hero Banner List UI. `HeroBannerPage.tsx` rewritten from a single-banner upsert form to a reorderable list (list-shaped DTOs/`HeroBannerApi`, `HeroBannerForm` in a `Modal` for add/edit, `HeroBannerRow` with up/down reorder + `ConfirmDialog` delete, image upload now edit-mode-only). `tsc`/`vite build` both clean; no live-backend verification this pass (none available in-session, consistent with Task Groups 11-15). See slipway/reports/default/task-group-16/developer-report.md.
- 2026-08-12 — scope-feature — feature 8: Product Grid Column Configuration approved (mirrored from ecom-os-be) — this repo's share is a bounded admin control (2-6) for product grid column count; the setting storage and public exposure are entirely `ecom-os-be`'s scope. Ready for `/shape-spec`.
- 2026-08-12 — scope-feature — feature 9: Product Badges (New Arrival, Trending, Discount) approved (mirrored from ecom-os-be) — this repo's share is three toggles on the Catalog product create/edit UI; the `Product` entity change and public exposure are entirely `ecom-os-be`'s scope. Ready for `/shape-spec`.
- 2026-08-12 — scope-feature — feature 10: Storefront Announcement / Promo Bar approved (mirrored from ecom-os-be) — this repo's share is an announcement text field + enabled toggle on `BrandingPage.tsx`; the Branding entity change and public exposure are entirely `ecom-os-be`'s scope. Ready for `/shape-spec`.
- 2026-08-12 — scope-feature — feature 11: Automatic Image Optimization on Upload approved (mirrored from ecom-os-be) — backend-only, no FE work implied: server-side resize/compress/format-convert on every upload through the shared `IFileStorageService`, deliberately no admin-facing toggle. Ready for `/shape-spec`.
- 2026-08-12 — shape-spec — task-groups: approved (2 groups: 17-branding-settings-extensions-ui, 18-product-badges-ui). Mirrors `ecom-os-be`'s task groups 18/19 — this repo's share is `BrandingPage.tsx` gaining a bounded grid-column select + announcement text/toggle (17), and `ProductEditorPage.tsx` gaining an edit-mode-only Badges section with three checkboxes (18). No FE task group for Feature 11 (backend-only). See slipway/memory/default/decisions.md for full reasoning.
- 2026-08-12 — implement — task-group-17: Branding Settings Extensions UI shipped. `BrandingPage.tsx` gained a "Storefront Display" section: a bounded (2-6) grid-column `<select>` and an announcement text input + toggle, the toggle disabled client-side whenever the text is blank (mirrors `ecom-os-be` Task Group 18's write-time rejection). `tsc --noEmit`/`vite build` both clean; no live-backend verification this pass (none available in-session, consistent with prior Storefront Configuration FE task groups). See slipway/reports/default/task-group-17/developer-report.md.
- 2026-08-12 — implement — task-group-18: Product Badges UI shipped. `ProductEditorPage.tsx` gained a "Badges" section (New Arrival/Trending/Discount checkboxes) between Specifications and Variants, gated `isEdit`-only (same precedent as SEO & Shipping) since badges are `PUT`-only server-side. `ProductDto`/`ProductUpdateDto` in `ProductsPage.tsx` extended accordingly; `ProductWriteDto`/create payload untouched. `tsc --noEmit`/`vite build` both clean. See slipway/reports/default/task-group-18/developer-report.md.
- 2026-08-13 — plan-product — prd: approved (9 features). `slipway/product/default/prd.md` replaced entirely, mirrored from `ecom-os-be`'s PRD replacement — now "Order, Cart & Checkout Management". This repo's own scope is narrow: only Feature 8 (Order Management admin UI — list/detail/status-update/refund-trigger) has any acceptance criteria here; Features 1-7 and 9 are `N/A for this repo` since every customer-facing surface (accounts, cart, checkout, payment collection) belongs to a new, separate, not-yet-created Next.js storefront application instead. See `slipway/memory/default/decisions.md` and `ecom-os-be`'s own decisions.md for full reasoning. Ready for `/shape-spec`.
