# Progress

- 2026-08-05 — plan-product — prd: approved (9 features)
- 2026-08-05 — shape-spec — task-groups: approved (10 groups: 01-taxonomy-brands, 02-core-product-publishing, 03-attribute-engine, 04-variants-merchandising, 05-multi-location-inventory, 06-pricing-tax-sales, 07-digital-products, 08-localization, 09-reviews-ratings, 10-storefront-catalog-api)
- 2026-08-05 — implement — task-group-01: Taxonomy/Category/Brand entities, admin API, and admin UI shipped on branch catalog/task-group-01-taxonomy-brands (both repos); 81/81 backend tests passing, FE typecheck/build clean.
- 2026-08-06 — implement — task-group-02: Product/ProductVariant/ProductMedia/ProductCategory entities, publish/archive workflow, media upload (local disk storage), and admin UI (ProductsPage + bespoke ProductEditorPage) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 121/121 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-02/developer-report.md (BE repo) for deviations (variant price/stock folded into product DTOs, added GET /products/types, first uploaded image auto-primary).
- 2026-08-06 — implement — task-group-03: Attribute engine (AttributeGroup/Definition/Option/Set/SetAttribute/ProductAttributeValue), category/product attribute-set binding, publish now blocks on missing required attributes, and admin UI (AttributesPage, AttributeSetsPage + builder, AttributeValueForm wired into ProductEditorPage's new Specifications section) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 150/150 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-03/developer-report.md (BE repo) for deviations.
- 2026-08-06 — implement — task-group-04: Variant-matrix generation (Cartesian over variant-defining attributes, idempotent), ProductOption/OptionValue, ProductBundleItem, ProductRelation, SizeChart/SizeChartRow, and admin UI (VariantMatrixGrid, options/bundle/relations sections in ProductEditorPage, SizeChartsPage) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 169/169 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-04/developer-report.md (BE repo) for deviations.
- 2026-08-06 — implement — task-group-05: Multi-location inventory (Warehouse, InventoryItem), retired ProductVariant.QuantityOnHand (data-preserving migration), and admin UI (WarehousesPage, InventoryPage with low-stock indicator; VariantMatrixGrid/ProductEditorPage now link out to Inventory instead of inline stock editing) shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 185/185 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-05/developer-report.md (BE repo) for deviations.
- 2026-08-06 — plan-product — PRD narrowed to pure product management/configuration scope at user request: Features 5 (Multi-Location Inventory), 6 (Pricing/Tax/Sales), 8 (Localization & Reviews), 9 (Storefront Catalog API) descoped from prd.md's forward-looking scope (both repos). Feature 5 was already implemented as Task Group 05 -- that code is untouched, only the PRD's active feature list changed. Feature 4 (Variants) trimmed in the PRD text to the SKU-matrix-generation acceptance criteria only; its already-shipped bundles/relations/size-charts remain in the codebase as delivered history. Removed now-orphaned task-group spec files 06/08/09/10 (never implemented) from slipway/product/task-groups/ in both repos.
- 2026-08-06 — security-review — Backend-only fix (no FE changes): a tenant-isolation audit found and fixed 9 cross-tenant IDOR gaps in catalog services (options, bundle items, relations, media, attribute options, size-chart rows) that validated a child row's FK against a URL parameter without ever confirming that URL parameter belonged to the current tenant. See slipway/memory/decisions.md (BE repo) for full details; BE suite 197/197 passing after the fix.
- 2026-08-07 — implement — task-group-07: Digital Products. ProductVariant.IsDigital, tenant-scoped DigitalAsset (unique per variant), GET/PUT /products/{id}/variants/{variantId}/digital-asset, PublishAsync now blocks a digital variant with no digital asset. Admin UI: Digital checkbox + Digital Delivery modal (file URL/name, max downloads, expiry days, license key pool id) added to VariantMatrixGrid. Shipped on branch catalog/task-group-01-taxonomy-brands (both repos, uncommitted); 203/203 backend tests passing, FE typecheck/build clean. See slipway/reports/task-group-07/developer-report.md (BE repo) for deviations.
- 2026-08-07 — qa — Full first QA pass across all 6 catalog task groups (01-05, 07), run in the BE repo -- reports at slipway/reports/task-group-<NN>/qa-report.md (BE repo). Real defects found: TG01 delete-with-products-attached returns 500 not 409; TG02 never implemented the PRD's Draft→Pending Review→Published lifecycle; TG03 has no FE UI at all to bind an attribute set to a category/product (API-only, its core deliverable is unreachable through the app); TG07 blank digital-asset fields return 500 not 400 (same missing-validation pattern as TG01). TG04/TG05 clean, no defects. See slipway/memory/progress.md (BE repo) for the full per-group breakdown.
- 2026-08-07 — implement — defect-fix pass: fixed every defect the QA pass above found. FE changes: CategoriesPage.tsx and ProductEditorPage.tsx now expose an attribute-set picker (category default, product override), closing TG03's "API-only, no way to actually use it" finding; PendingReview status + a "Submit for Review" action added to ProductsPage.tsx and ProductEditorPage.tsx (TG02's missing lifecycle step); explicit client-side file-type check on image upload (TG02 minor); attribute number inputs now show/enforce their min/max bounds (TG03 minor); a delete action was added to TaxonomiesPage.tsx now that the backend endpoint exists (TG01). Backend-side fixes (exception mapping, delete guards, PendingReview status/endpoint) are in the BE repo -- see its progress.md/decisions.md. Typecheck and build both clean. Uncommitted.
- 2026-08-07 — plan-product — prd: approved (5 features). slipway/product/prd.md (both repos) replaced entirely -- now the Storefront Configuration Module PRD (Branding & Site Identity, Content Pages, Product Showcases/Collections, Homepage Hero Banner, Navigation Menus), not Catalog. Catalog's own PRD content is superseded going forward but all its shipped code, task-group specs, and developer/QA reports remain untouched as historical record (same pattern as the 2026-08-06 Catalog rescoping). Deliberately excludes a public read API and the storefront frontend itself, both deferred to when that project starts, mirroring Catalog's own Feature 9 deferral. tech-stack.md updated to match (both repos).
- 2026-08-07 — shape-spec — task-groups: approved (5 groups: 11-storefront-branding, 12-content-pages, 13-product-showcases, 14-homepage-hero-banner, 15-navigation-menus). Numbered from 11 to avoid colliding with Catalog's historically-referenced-but-deleted task-group numbers. New StorefrontConfigModule.cs introduced rather than reusing the existing Menu/SiteSetting entities (those configure the admin app itself, not the tenant's storefront). See slipway/memory/decisions.md (BE repo) for the full design reasoning, including the shared LinkTargetType approach Task Groups 14/15 use.
