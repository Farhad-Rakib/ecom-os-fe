# Progress
- 2026-09-09 — plan-product — prd: approved (3 features). Mirrored from `ecom-os-be`'s `fulfilment-readiness-prd` (6 features), which is the source of truth for the product. This repo's scope is the three admin surfaces the backend work needs: payment method + COD settlement on the order views, stock movement visibility on the inventory views, and the email settings/templates/delivery-log screens plus a per-order notification panel. Storefront surfaces (checkout payment selection, the out-of-stock refusal) are out of scope here — this repo is the back office. **`OrdersPage.tsx`'s standing obligation is triggered**: backend task group 03 makes `PendingPayment → Shipped` legal for COD orders only, so this repo's mirrored transition table now needs the order's payment method, the first time that mirror has depended on anything but the status.
- 2026-09-09 — shape-spec — task-groups: drafted (3 groups: 01-payment-method-and-cod, 02-stock-movement-visibility, 03-email-admin). Each depends on its backend counterpart being built first, per this platform's standing backend-first sequencing. Awaiting review.
- 2026-09-09 — shape-spec — task-groups: approved (3 groups: 01-payment-method-and-cod, 02-stock-movement-visibility, 03-email-admin). Approved alongside the backend's 5 groups. Each waits on its backend counterpart, per this platform's standing backend-first sequencing: FE 01 needs BE 01+03, FE 02 needs BE 02, FE 03 needs BE 04+05.
- 2026-09-09 — implement — task-group-01: Payment Method & COD on the Order Views built end to end. Payment column + filter on `OrdersPage`, payment method and a COD panel (amount due, settled state, full settlement history with the authoritative record marked, Mark collected / Reverse behind a confirm) on `OrderDetailPage`, new `OrderSettingsPage` (`/orders/settings`, `orders.settings.manage`) and `UnsettledCodPage` (`/orders/cod`), and `DispatchPanel` updated for the COD cash amount. **`OrdersPage`'s standing obligation discharged**: `allowedTransitionsFor(status, paymentMethod)` added as a separate COD-only table rather than widening `ALLOWED_TRANSITIONS`, which would have offered `PendingPayment → Shipped` for card orders the backend always rejects. **Also fixed a pre-existing compile error**: `OrderDetailPage` referenced a `recipientRating` defined nowhere — invisible because the root `tsconfig.json` has `"files": []`, so a bare `tsc --noEmit` checks nothing and only `npm run typecheck` (which targets `tsconfig.app.json`) catches it. Uncommitted on `dev`.
- 2026-09-09 — implement — task-group-02: Stock Movement Visibility built. New `StockMovementsDrawer` (per-variant history, each order-driven row linking to its order, manual corrections visually distinct), `Reserved` promoted to a real column, and **negative availability rendered as negative and flagged "oversold"** rather than clamped — the synced-oversell case is the one this exists for. **One acceptance criterion is NOT met**: the backorder indicator on `OrderDetailPage` (FE Feature 2 AC6) needs a per-line backorder flag on `OrderDetailDto` that the backend does not expose; the shortfall is still visible on the inventory views and in the stock history. Uncommitted on `dev`.
- 2026-09-09 — implement — task-group-03: Email Configuration, Templates & Delivery Log built. Three new pages under `notifications.manage` plus a shared `notifications.api.ts`. The SMTP password follows `CourierConnectionPage`'s credential pattern exactly — never populated from the server, cleared after save, blank keeps the stored secret. Platform-default banner and revert; send test surfacing the server's own reason; template editor listing each template's tokens; delivery log filterable by status and order with retry, `Abandoned` styled apart from `Failed`. `npm run typecheck` and `npm run build` clean; all new files lint with zero errors. **One pre-existing error remains outside scope**: `storefront/pages/BrandingPage.tsx:293` has failed `npm run typecheck` since commit `ffc4539`. Uncommitted on `dev`.
- 2026-09-09 — implement — follow-up fixes: (1) **`storefront/pages/BrandingPage.tsx` now compiles** — its social-links loop typed the field as `keyof FormState`, which admits `productGridColumns` (number) and `announcementEnabled` (boolean), letting a boolean reach an `<input value>`. Narrowed with a `StringFieldOf<T>` mapped type. That was the last error in `npm run typecheck`, which **now passes repo-wide for the first time** in this variant's work. (2) **FE Feature 2 AC6 closed** — backend `OrderLineItem.IsBackordered` added and surfaced as a per-line "Backordered" badge on `OrderDetailPage`. Lint errors 212 → 211; no new ones introduced.
