# Progress

- 2026-08-19 — plan-product — prd: approved (1 feature). New variant `order-management-prd` ("Order Management") created in this repo — admin UI for the already-implemented `ecom-os-be` `order-management-prd` backend (task-group-01, committed `5d40bb7`). Single feature: `OrdersPage.tsx`/`OrderDetailPage.tsx`, manual status-update mirroring the backend's `OrderStatusTransitions.AllowedFrom` matrix, status control hidden entirely for externally-sourced orders (`customerId: null`) rather than relying on the backend's 400. Deliberately written to be agnostic of `store-sync-prd`'s existence — that variant will amend these pages later for a synced-order indicator, not the other way around.
- 2026-08-19 — shape-spec — task-groups: approved (1 group: 01-order-management). `OrdersPage.tsx`/`OrderDetailPage.tsx` in `src/features/orders/`, reusing the shared `DataTable` component and `BaseRepository` API-class convention (matches `ProductsPage.tsx`). Backend's `OrderStatusTransitions.AllowedFrom` matrix hand-mirrored client-side as a maintenance-flagged constant. Full reasoning in `decisions.md`.
- 2026-08-19 — implement — task-group-01: done, uncommitted. Added `src/features/orders/pages/OrdersPage.tsx` (DTOs/`OrderApi`/`orderApi`/`ALLOWED_TRANSITIONS`/`statusBadgeClasses` inline, matching `ProductsPage.tsx`) and `OrderDetailPage.tsx` (imports from `OrdersPage.tsx`, status-update section gated on `customerId !== null`); registered `orders`/`orders/:id` routes in `src/app/router/index.tsx` under `<PermissionGuard permissions={['orders.manage']}>`. Added an optional `onRowClick` prop to `src/components/table/DataTable.tsx` (backward-compatible) since the shared table had no row-click capability, needed for row-click-to-detail. Discovered this repo has no static nav config file — the sidebar is entirely backend-driven (`GET /Menu`, managed via the existing `features/menu/` Menu Management admin page) — so no "Orders" sidebar entry was added in code; it requires a `MenuItem` row (`title: "Orders", url: "/orders", icon: "orders", requiredPermission: "orders.manage"`) created via that admin page or a backend seed, both outside this repo's/task's scope. `tsc --noEmit` and `vite build` both clean for every file this group touched (one pre-existing, unrelated `tsc` error in `BrandingPage.tsx`, confirmed identical to `dev` HEAD). Full detail in `slipway/reports/order-management-prd/task-group-01/developer-report.md`.
